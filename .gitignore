node_modules/
dist/
*.tsbuildinfo
.env
.env.local
# SQLite and its WAL sidecars — captured events never belong in the repo
*.sqlite
*.sqlite-*
services/collector/data/
.expo/
coverage/
.DS_Store

# Expo continuous native generation — regenerate with `npx expo prebuild`
apps/mobile/ios/
apps/mobile/android/
